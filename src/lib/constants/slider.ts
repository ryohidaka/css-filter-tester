// Import FilterStep enum from './filter'
import { FilterStep } from './filter';

// Default minimum value for sliders
export const SLIDER_DEFAULT_MIN = 0;

// Default maximum value for sliders
export const SLIDER_DEFAULT_MAX = 100;

// Default unit (empty string for no unit)
export const SLIDER_DEFAULT_UNIT = '';

// Default step type for sliders (using the FilterStep.Integer enum value)
export const SLIDER_DEFAULT_STEP = FilterStep.Integer;
